Efuelite Solutions RandomHelper helps to generate random numbers as string used for One time password 
It generates from 3 digits to 30 digits.

Usage:

namespace EfueliteSolutionsRandomHelper;

var oTP = OTP.NewOTP(6) // 6 digits random number string will be generated. etc.
	
For further enquiries, you can send mails to info@efuelite.com